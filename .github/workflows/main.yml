name: CI

on: [push, pull_request]

jobs:
  Build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - uses: OlivierLDff/get-cmake@latest

      - name: Configure
        run: mkdir -p build && cmake -DCMAKE_BUILD_TYPE="Release" -B build -S .