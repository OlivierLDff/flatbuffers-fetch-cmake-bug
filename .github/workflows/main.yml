name: CI

on: [push, pull_request]

jobs:
  Build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - uses: lukka/get-cmake@3.17.1

      - name: Configure
        run: mkdir -p build && cmake -DCMAKE_BUILD_TYPE="Release" -B build -S .